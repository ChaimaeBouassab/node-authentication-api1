# Database connection
PORT=YOUR_PORT  # Replace with the port your API should listen on
MONGO_URI=YOUR_MONGO_URI  # Replace with your MongoDB connection URI

# Server configuration
PORT_REDIS=6379  
HOST='127.0.0.1' 

# Security secrets (replace with random, strong values)
ACCESS_TOKEN_SECRET=REPLACE_WITH_STRONG_RANDOM_VALUE
REFRESH_TOKEN_SECRET=REPLACE_WITH_STRONG_RANDOM_VALUE

